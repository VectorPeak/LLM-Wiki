---
title: "微信_阿东玩AI_Agent_公众号文章剪藏_2026-05-29_1-2"
source: "/api/v1/wechat_mp/web/fetch_mp_article_detail_json"
author:
  - "阿东玩AI"
published: "2026-05-29"
created: 2026-05-29
description: "TikHub 命中的微信公众号文章候选，共 2 条，本文档收录 2 条"
tags:
  - "clippings"
  - "wechat"
  - "阿东玩AI"
---

## 0x01. 我观察了一年，发现90%的Agent教程一开始就教错了
> 发布日期：2026-05-29  
> 原文链接：[我观察了一年，发现90%的Agent教程一开始就教错了](https://mp.weixin.qq.com/s/UKkYx_nmxwu5TQIogV2uyw)

大部分Agent教程，第一节课都在教你"怎么调LLM API"、"怎么写prompt"、"怎么function calling"。

这是教错地方了。

调API是5行代码的事。写prompt是LLM自己就能教你的事。function calling文档官网就有。这些都不难，也不是搭Agent真正卡住人的地方。

真正卡住人的，是一件大多数教程根本没提过的事——harness工程。

harness是什么？先用一个比喻讲透

harness这个词，直译是"马具、挽具"，就是套在马身上让你能驾驭它干活的那套皮带、嚼子、缰绳。在AI Agent圈，它指的是围绕模型的那一整圈工程外壳——loop、tools、memory、channels、cron、permission、sandbox、provider抽象层。

Andrej Karpathy说过一句很到位的话："Agency comes from the model. The harness makes agency real."(智能来自模型，harness让智能落地。)你买的Claude/GPT订阅是引擎，harness是车架、变速箱、方向盘、油门刹车。光有引擎你只能盯着它转，套上harness它才能拉你去任何地方。

把Agent想成一只新来的实习生。

模型本身(Claude/GPT-5/DeepSeek)相当于这个实习生的智商。智商在哪个公司都一样——他记性好不好、推理强不强，是天生的。

但同一个智商的实习生，在A公司一周搞瘫一个项目，在B公司一个月独立带项目跑——区别在哪？

区别在公司给他配的工具：

- 是不是有工位(loop)：他有没有一个稳定的"上班"状态，每天来、每天走、每天能继续昨天的事
- 是不是有权限(tools)：他能不能开公司邮箱、查内部数据库、订机票
- 是不是有记事本(memory)：他记不记得"上周这个客户已经被王哥跟了"
- 是不是有名片(SOUL)：他对外是不是稳定的"我们公司的人"，而不是今天像销售、明天像客服
- 是不是有手机(channels)：客户能不能在微信、飞书、邮件任意一个地方找到他
- 是不是有日程表(cron)：他能不能自己安排"明天早上9点提醒我开会"
- 是不是有审批流程(permission)：他能不能自己花10万块？还是必须先报备？
==这一整套东西就是harness。== 同样一个Claude模型，套上一套好harness，就是你每天都在用的系统；不套harness，就只是个聊天窗口。

你不会训练模型没关系，你也不需要训练模型。模型是Anthropic/OpenAI帮你养好的。你要做的，是给这个已经很聪明的实习生配一套合身的harness。

harness的七层(小白只需要先搞前三层)

我把harness拆成七层，从底到顶：

好消息是：小白第一周只需要把L1+L2+L3跑通。L4–L7等你真的开始用、真的有需求了再加。一开始就想搭七层是新手最大的陷阱。

L1 模型层：永远写provider抽象

今天用Claude，明天OpenAI出GPT-6你想换，后天某家API涨价你想切到DeepSeek。如果代码里到处写死anthropic.messages.create(...)，每次切换都是地狱。

最佳实践：所有LLM调用过一个统一的call_model(messages, tools)函数，具体哪家由配置文件决定。新手做法：找一个内置multi-provider的Agent基座，就直接送你这一层。

L2 循环层：一个while + 一个stop_reason

单次问答只能干一步事，Agent之所以是Agent，就是因为它能"想一下→动一下→看结果→再想一下"。核心循环就这么短：

所有看起来很玄乎的ReAct、Plan-and-Execute、Reflexion，说到底都是这个循环加点装饰。新手能在自己项目里指着agent loop说"就是这一段"，你已经超过80%写过Agent的人了。

L3 工具层：dispatch表 + 默认沙箱

让Agent不只能聊天，还能搜网页、读文件、改代码、发消息。两个最佳实践：

第一，用dispatch表，不要用if-elif长链。每个工具是一个{name, description, schema, handler}的字典，注册到一张表里，新增工具是加一行，不是改一坨。

第二，高风险工具默认走沙箱或者审批。读文件OK，写文件最好限定在某个工作目录，删文件、发邮件、调外部API这种应该走"先dry-run给你看一眼，你点头才执行"的流程。这是新手最容易出事的一层——千万别给一个刚跑通的Agent直接rm -rf权限。

L4–L7 先不用管

记忆层：第一版用sessions/{user_id}.jsonl，每次对话末尾追加一条，下次启动读最近N条塞回context，能解决90%的需求。Pinecone、Weaviate这些向量库都很好，但新手过早引入带来的复杂度会让迭代速度立刻降一半。

人格层：写一个SOUL.md文件，定义Agent的人格、说话风格、价值观。每次调模型时把它注入system prompt的第一段。模型切换之后回答风格的稳定性立刻上一个档次。

通道层和自驱层：等你真的在用Agent了再加。

7天最小可用harness的实战路径

目标是7天后你有一个真的能用、真的进入你日常的Agent。不是demo，是工具。

Day 1：准备 + 选基座

装Claude Code(npm install -g @anthropic-ai/claude-code)。注册Anthropic账号，订阅Pro或者去console.anthropic.com拿API Key。

挑一个Agent基座，给小白推荐两个：

- Nanobot(github.com/HKUDS/nanobot)：内置multi-provider、memory、skills、IM通道，直接送你L1+L4+L6，新手最省心
- pi-mono(github.com/badlogic/pi-mono)：极简风格，几百行能看懂全部，适合想从底层理解的程序员
不会写Python的选Nanobot。

Day 2：跑通安装

把基座的GitHub链接直接丢给Claude Code："请帮我把这个项目fork到本地，安装好，要求能正常运行。一步步告诉我每一步要做什么，遇到报错继续帮我排查。"

⚠️ API Key安全提醒：永远不要直接在命令行里贴API Key。命令行会留history，截图、屏幕分享都可能泄露。让Claude Code告诉你"应该写在哪个文件、什么格式"(通常是.env)，自己用编辑器手动填。

Day 3：接通第一个IM通道

海外用Telegram最快：找@BotFather发/newbot，给bot起名字，拿到Token填进nanobot配置文件，5分钟搞定。

国内用飞书：去open.feishu.cn/app创建企业自建应用，启用"机器人"能力，申请im:message等权限。事件订阅一定要选"长连接模式"——不需要公网IP、不用配webhook。最后一步——点"版本管理与发布"把应用真的发版，不发版权限不生效。这是90%新手卡住的地方。

Day 4：建脚手架文件

不要急着加新功能。今天花一小时建脚手架文件——它是后面所有迭代速度的地基。

项目根建一个CLAUDE.md(< 200行)。里面写四块：WHAT(项目一句话讲清)、WHY(哪些设计选择不能动)、HOW(怎么build、怎么test)、个人偏好(用中文回复/commit前先问)。

再建一个SOUL.md：定义Agent的人格。这是L5的全部——一个文件，几百字，模型切换之后风格不漂移。

Day 5：加第一个真的有用的功能

关键的反直觉点：不要从"我想要什么"开始，要从"我每天最烦的一件事"开始。

"我想要它能联网搜索"——错。这是大模型客户端都有的功能。"我每天早上要花20分钟看GitHub trending、Hacker News和X的AI推文"——对。这是你的真实痛点，自动化它一次性省回30小时。

挑一个，然后跟Claude Code进入plan mode(按Shift+Tab或者输入/plan)，告诉它你的需求，让它反复追问你直到把触发方式、抓取范围、推送格式、失败处理都问清楚，最后写到docs/spec.md。

==Plan mode是Claude Code内置的"只规划不写代码"模式。它会一轮轮问你，问到你自己脑子里都没想清楚的细节。花30分钟brainstorm，能省后面两周的乱改。这是整套流程里性价比最高的一步。==

Day 6：让它真的进入你的日常

今天的目标不是写代码，是用。把昨天加的功能跑起来，连续用一整天。关键是发现两类问题：它哪里做错了，以及它哪里做对了你没想到。

Day 7：开始建第二个能力，并复盘

到这一天，你已经有一个跑得通、每天在用的Agent了。这本身已经超过95%看过Agent教程的人——他们停在了demo，你已经进了生产。

新手最常见的四个误判

误判一：先选最强的模型。错。模型差异在Agent工程里其实很小——同一套harness套Claude Sonnet 4.5和GPT-5，体验差异远小于你套了好harness和坏harness的差异。先把harness搭好，模型这一层后面5分钟就能换。

误判二：先想清楚架构再动手。错。Agent是一个反馈密集型项目，你想清楚的80%在你真用起来后会被推翻。先丑后美，先跑后优。

误判三：找最好的框架。错。LangChain的抽象是为了支持上百种LLM、上百种向量库，对你一个人完全是冗余。轻量基座 + 自己长的能力 = 长期最舒服。

误判四：上来就追多模型/多通道/多功能。错。一个模型 + 一个通道 + 一个真的有用的功能，跑顺一个月，你学到的东西比横跨5个模型的人多10倍。

经常有人问我："我不会写代码，搭Agent是不是太难了？"

我的回答每次都一样：搭Agent不是写代码，是搭harness。模型的智能不是你能造的，但harness是。说到底这是工程审美——什么放哪一层、什么用配置、什么用代码、什么先做、什么后做。跟你会不会Python关系不大，跟你愿不愿意每天跟它对话、每天调一调有关。

"Agent"这个词不神秘。它就是一个套了harness的LLM。

七天后，你会有一个真的进入你日常的工具。不是demo，是工具。

阿东，大模型算法工程师，OPC创业者。

## 0x02. 做了一年Agent，这8个坑我都踩过
> 发布日期：2026-05-29  
> 原文链接：[做了一年Agent，这8个坑我都踩过](https://mp.weixin.qq.com/s/XJgmovvR8NJqar6fjAwmaQ)

跑通第一个Agent之后，你会进入一个奇怪的阶段——感觉哪里不对，但说不清楚问题叫什么名字。

这篇就是给这个阶段的人写的。还没跑通第一个Agent的，先看上一篇入门篇。

进阶一：context engineering才是真正的护城河

你的Agent跑十轮就开始胡说八道，长任务做不完，token账单暴涨。

这不是模型不行，是你context管得烂。

核心认知：模型的智商是固定的，你能改的只有"在每个turn给它看什么"。这件事现在有个专门的名字叫context engineering，2025年下半年开始取代prompt engineering成为Agent工程的核心话题。

==几个必须建的机制：把每次塞给模型的prompt拆成稳定的几层——system层(SOUL.md + 不变的工程规则)、long层(项目状态、最近N天retro)、short层(当前任务的spec、todo)、turn层(当前这一轮的user message + 工具结果)。每一层的更新频率不同，核心是知道哪些字段是"贴的"哪些是"算的"。==

另外两个被低估的点：

工具结果不要原样返回。一个read_file返回8000 token的代码塞回context，三轮就废了。中间套一层result postprocessor，根据当前任务把无关部分剪掉，只留"模型下一步真正需要的"。

cache友好的prompt结构。Anthropic和OpenAI都支持prompt caching，但前提是前缀稳定。把变化的部分(时间戳、随机id)全部挤到prompt末尾，前面90%走cache，账单立刻降一半。这件事很多人不知道，但跑量大的时候差别巨大。

进阶二：multi-agent的边界——什么时候该分

看了一圈AutoGen/CrewAI的multi-agent demo觉得很酷，自己一上手发现极其难调。

这是社区目前最大的分歧。Cognition(造Devin的那家)发了一篇文章《Don't Build Multi-Agents》，立场是"绝大多数场景应该用单Agent + 长上下文"。Anthropic几乎同时发的《How we built our multi-agent research system》立场是"复杂研究任务确实需要orchestrator + subagent模式"。

两边都对，关键看你的任务有没有"天然可并行的子问题"。我自己的判断标准是一句话：

子任务之间需要频繁交换中间状态，用单Agent；子任务可以独立完成、最后只汇总结果，才考虑multi-agent。

multi-agent的真实工程代价很多人没算清楚：context在agent间传递会失真；每个Agent 90%准确率，串5个就剩59%；出错的时候你要看5个trajectory而不是1个。

我的判断是：默认单Agent，只有当任务存在天然并行性、且子任务输出可以被独立验证时才上multi-agent。架构上推荐supervisor模式(一个主Agent派发任务给若干worker，worker不互相通信)，尽量避免swarm模式——后者几乎无法调试。

进阶三：tool design的工程哲学

工具数量从10个涨到50个之后，Agent开始选错工具、参数填错、调一半hang住。

tool design是API design的子集，但多了一个约束——消费者是LLM，不是人。

LLM跟人类工程师有几个关键差别，每个都对应一条设计准则：

LLM没有"试一下看看"。人类API调错了报错改改再来，LLM调错了整个trajectory就乱了。所以每个工具的description必须能让模型在调用前就95%确定它在调对的工具，要包含：什么时候用、什么时候不用、和相邻工具的边界、典型例子。

LLM容易选最显眼的。search和web_search同时存在，模型30%概率选错。强制工具名互斥语义——search_codebase/search_web/search_docs，让名字本身就能消歧。

LLM没有"分页"概念。一个工具返回10000行，模型不会自己说"给我下一页"，它会试图全部塞进context。所有可能返回大量数据的工具必须强制分页或截断。

LLM会幻想参数。你没写在schema里的字段它也会填。schema要严格JSON Schema，enum字段优先，开放字符串字段最后用。

两个反复出现的高价值工具：execute_code(让Agent写一段代码去调度其他工具，把"调5次工具"压成"调1次脚本"，token省一半)；think/scratchpad(一个不产生任何side effect的工具，唯一作用是给模型一个"想一下"的回合，对长任务效果显著)。

进阶四：memory真正的难点不是存，是召回

你接了Pinecone/Weaviate，存了所有对话，但模型还是不知道你昨天说过什么。

存不是问题。memory工程真正的难点是"什么时候该召回、召回什么、召回多少"。

几个我觉得被低估的点：

主动memory比被动memory重要。被动是"用户问了我去查"，主动是"在用户开始问之前我就把相关记忆塞进context"。实现：每个turn开始前跑一个轻量retrieval，结果塞进long层context。

memory要分写入门槛。不是所有对话都值得存进长期记忆——99%的闲聊存了反而是噪声。设一个"值得记住"的判断器(用一个便宜模型当gate)，只把高信号的内容写进长期记忆。

memory必须支持遗忘和合并。同一件事说三次，要合并成一条；过期的偏好要更新或删除。没有遗忘机制的memory，半年后就是污染源。

实操建议：第一版用SQLite + JSONL，加一个简单的keyword索引，能覆盖80%的需求。等你真的卡到容量瓶颈(一般是百万级turns之后)再上向量库。过早引入向量库是中级开发者最常见的过度工程。

进阶五：evals是分水岭

你换个模型、换个prompt、改个工具，怎么知道是变好了还是变差了？

99%的Agent项目没有eval，所以任何修改都是"感觉变好了"。这是中级和高级Agent工程师的分水岭。

Anthropic把eval拆成三层：

Component eval(每个工具、每个prompt单元测试级别的eval)：每个工具一组fixture，典型输入→期望输出，commit前必跑。

Trajectory eval(看一整段agent loop的中间过程)：不是只看最终输出，而是看"用了几步、每步选的工具对不对、有没有走弯路"。用LLM-as-judge给trajectory打分。这一层最难做，但杠杆最大。

End-to-end eval(任务级别)：一组真实任务，定义成功标准，每周跑一次，盯一个长期曲线。

实操建议：每个项目至少配component eval + 20个end-to-end case。trajectory eval是奢侈品，先把前两个做扎实再说。

进阶六：可靠性工程——让Agent在生产里活下来

本地跑得好好的，上了生产三天就出问题。

生产环境的Agent跟demo的Agent不是一个东西。下面这些是必须有的：

Idempotency：每个工具调用必须带idempotency key，因为LLM会重试同一个调用。"发送邮件"这种工具如果不做幂等，重启一次能给客户发5封一样的邮件。

Cost guard：每个trajectory设token上限和金额上限，超过就硬停。这一条非常重要。一个bug让Agent进入无限循环、半小时烧光你1000美金的故事每个月都在发生。

Permission tier：把工具按风险分级——读类工具(低风险，自动执行)、写类工具(中风险，dry-run + 人审)、不可逆工具(高风险，必须显式确认)。

可观测性：每个trajectory至少要能事后回放——记录每一步的prompt、tool call、tool result、token使用、延迟。Langfuse、Helicone这类工具值得接，自己用JSONL落盘也行，但绝对不能没有。

进阶七：几个非显然的工程选择

框架选择：LangGraph适合显式状态机的工作流，AutoGen适合多agent对话，CrewAI适合角色分工任务。但2025年的事实是：自己写一个轻量harness比用任何框架都稳。框架的抽象总是为了"通用性"，对你单一项目就是噪声。除非你团队在做平台、给别人用，否则别选框架，选轻量基座。

协议选择：MCP(Model Context Protocol)是Anthropic推的工具协议，已经被Claude Code、Cursor、OpenAI的Agents SDK采用。新工具优先实现成MCP server，跨agent runtime复用，而不是绑死在某个框架的plugin系统里。

模型分工：一个生产Agent用3个不同价位的模型分工是当前的成熟做法——主推理用Claude Sonnet 4.5+，长context summarizer用Gemini(128k+ context价格优势明显)，低成本gate/classifier用Haiku或DeepSeek。

部署：个人Agent不要上K8s，一个systemd service + 一个SQLite就够。从复杂度起步是中级开发者第二常见的过度工程。

进阶八：必读清单

按"读完真能改你工程做法"的顺序排：

第一档(必读)：
第二档(看源码)：
第三档(学术)：
每一档读完都能改你写Agent的方式。第一档不读，你就是在重复发明很多人已经踩过的坑。

说实话，这8个专题我自己也还在持续踩坑。context engineering这块我现在的实现还很粗糙，evals也只做了component eval，trajectory eval一直没时间做扎实。

但这些坑的名字，我现在至少能叫出来了。

叫得出名字，就能搜到答案。

阿东，大模型算法工程师，OPC创业者。
