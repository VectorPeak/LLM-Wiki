# 【第四周】秋招必杀项目 - Functioncall微调系列（训练策略+评估）

## 前言

对于初学者而言，这一节需要先理清两件事：

1. 最终传给 LLM 的到底是什么，`tool_call` 和没有 `tool_call` 这个参数的区别是什么。
2. 多轮对话里的 loss 到底是怎么 mask 的。

下面这一组内容，核心就是围绕这两个问题展开，并进一步落到训练策略与评估方式上。

## 示例：一条典型的酒店推荐链路

下面这条样例是本节问题分析的起点。模型先调用 `recommend_hotels`，再调用 `get_hotel_reviews`，最后才生成自然语言回复。

```text
<tool_response>
根据您的需求，推荐以下酒店：

酒店1：武汉汉口江滩美居酒店
位置：武汉市江岸区沿江大道159号

</tool_response><|im_end|>
<|im_start|>assistant
<tool_call>
{"name": "get_hotel_reviews", "arguments": {"hotel_name": "武汉汉口江滩美居酒店"}}
</tool_call><|im_end|>
<tool_response>
武汉汉口江滩美居酒店的用户评价：

评价1：
评分：4/5
评论：酒店位置很好，房间干净整洁，服务人员态度友好。早餐种类丰富，性价比不错。

评价2：
评分：5/5
评论：商务设施齐全，会议室很专业，网络稳定。房间安静，适合工作和休息。

</tool_response><|im_end|>
<|im_start|>assistant
<think>
</think>

为您推荐的酒店是：**武汉汉口江滩美居酒店**，以下是详细信息：

- **位置**：武汉市江岸区沿江大道159号
- **特点**：酒店位置优越，临近江滩，房间安静舒适，适合休闲和商务出行。

### 用户评价：
1. **评分**：4/5
   **评论**：酒店位置很好，房间干净整洁，服务人员态度友好。早餐种类丰富，性价比不错。
2. **评分**：5/5
   **评论**：商务设施齐全，会议室很专业，网络稳定。房间安静，适合工作和休息。

如果您有其他需求或想查看更多酒店，请告诉我！
<|im_end|>
```

---

## 1. 策略

### 1.1 第一次问题分析

> 问题一：模型在最后一步混淆 Reply 与 Tool Call

这个问题的本质是：模型**无法准确判断“任务是否已经完成”**。  
它知道如何按部就班地调用工具，但在获取了所有必要信息后，它不确定应该把信息汇总成自然语言回复给用户（生成 `reply`），还是认为还需要调用另一个工具（生成 `tool_call`）。

#### 可能的原因分析

1. 数据集中“任务结束”的信号不足（最可能的原因）

- 现象：你的训练数据中，绝大多数样本可能都是“中间过程”，即 `user -> tool_call -> tool_response -> tool_call -> ...`。  
  而明确展示“在获取所有信息后，最终进行总结回复”的样本比例过少。图片里给出的经验判断是：**5/6 的数据都有 tool_call，而且调用得好、轮子多，模型就更倾向于生成 tool_call**。
- 原因：模型在训练时，看到的大部分情况都是在 `tool` 角色消息之后，紧接着生成一个 `assistant` 角色的 `tool_calls`。它学到了一个很强的模式：只要看到工具结果，下一步就是调用新工具。
- 解决方案：
  - 增加“终结”样本：人工构造或筛选一批数据，这些数据的最后一轮对话必须是 `assistant` 生成**纯文本 `content`**，而不是 `tool_calls`。
  - 样本示例：像上面的酒店推荐样例，最后一条 `assistant` 回复应该是自然语言总结，而不是再发起一次工具调用。
  - 强化信号：确保这些终结样本的 `content` 足够高质量，并且明确体现“我已经完成了你的请求，这是最终答案”。

2. 数据存在歧义或冲突

- 现象：在相似的上下文中，有些样本以 `tool_call` 结尾，另一些却以 `reply` 结尾。
- 原因：模型会被这些矛盾信号搞糊涂，学不到清晰的决策边界。
- 解决方案：
  - 数据审查：仔细审查你的 2000 条生成数据，确认任务完成的逻辑保持一致。
  - 统一规则：例如明确规定，**当且仅当所有必需信息（如风险位置、合规条款）都已通过工具获取后，才生成最终 `reply`**。
  - 清理逻辑不一致的数据。

3. System Prompt 的引导性不强

- 现象：系统提示词可能只描述了模型的能力，但没有明确指示最终目标。
- 原因：System Prompt 对模型行为有全局性引导作用。如果提示语只是“你是一个能调用工具的助手”，模型就会更倾向于持续展示工具调用能力。
- 解决方案：
  - 优化 System Prompt：加入明确指令，强调最终目标。  
    例如增加一句：**“你的最终目标是利用工具收集所有必要信息后，向用户提供一个完整、清晰的总结性答复。”**

> 问题二：有时会卡死半小时都生成不了结果

这里的“卡死”通常不是真正的死机，而是模型进入了一种**无限生成循环**，或者**无法触发停止条件**的状态。

#### 可能的原因分析

1. 模型陷入生成循环（最可能的原因）

- 现象：模型可能在内部不断生成重复、无意义的 token，或者在几个特殊 token 之间来回振荡。截图里提到的典型现象包括反复生成 `<|im_start|>assistant`，后面还有 `<tool_call>` 连续出现的情况。
- 原因：
  - 数据污染：训练数据里可能混入了错误格式或不完整对话，导致模型学到错误“结尾”模式。
  - 未能学会生成 EOS Token：如果 SFT 后模型没有稳定学会在一轮对话结束后输出结束信号，推理时它就可能一直生成到 `max_new_tokens` 上限。
- 解决方案：
  - 数据清洗：优先使用 `inspect_qwen_dataset.py` 检查经过 `apply_chat_template` 之后的文本末尾是否干净、完整。
  - 检查推理参数：在调用 `model.generate()` 时设置合理的 `repetition_penalty`（如 `1.1`），先作为诊断手段观察是否能打破简单循环。

2. 推理时停止条件配置错误

- 现象：推理代码可能在等待一个永远不会出现的停止信号。
- 原因：
  - `eos_token_id` 未正确设置。
  - 使用了自定义停止逻辑，但模型微调后输出习惯改变，导致原来的停止条件再也触发不到。
- 解决方案：
  - 打印并核对 `tokenizer.eos_token_id` 与 `model.generation_config.eos_token_id`。
  - 同时使用 `eos_token_id` 与 `stop_strings`（例如 Qwen 的 `<|im_end|>`）。
  - 增加超时机制，防止服务长时间挂起。

3. 上下文窗口超出限制

- 现象：当对话轮次过多时更容易出现。
- 原因：随着对话进行，`input_ids` 长度不断增加，接近甚至超过模型最大长度限制（如 `max_seq_length`）后，模型行为会变得不可预测。
- 解决方案：
  - 做上下文截断或滑窗管理，确保输入 token 数量始终在安全范围内。

### 1.2 不平衡样本的数据处理：不会 Reply 怎么办

这一部分给出了两个办法：

1. 第一轮训练后再单独训练 `reply`
2. 在第一轮训练中解决数据不平衡

课程里最终更认可第二种，也就是在第一轮训练阶段就做数据预处理。

#### `conversation_splitter.py` 的核心思路

在第一轮训练阶段，先对数据进行预处理：

- 对于每条 `assistant` 数据，都从最开始的 `system_prompt` 到本轮 `assistant` 回复截取一份序列。
- 如果该回复属于 `reply` 类型，就将其复制三份，用来缓解 `reply` 与 `toolcall` 数据分布不平衡的问题。
- 训练时，每条序列只计算**最后一个 `assistant` 回复**的 loss。

#### 这个办法为什么有效

1. 解决了“任务结束”信号不足的问题

- 原始问题里，一个长轮次多轮对话中，模型可能只在最后才看到一次“任务结束并总结”的信号，在中间阶段学到的更多是“继续调用工具”。
- 通过把最后一轮对话（通常是生成 `reply` 的那一轮）重复三次，可以显著提高这类“任务结束”信号在整个训练集中的权重和出现频率。

2. 提供了“中间监督”，增强模型鲁棒性

- 逐轮拆分之后，模型能看到任务在不同阶段分别长什么样。
- 它会学到：只有一轮工具调用时该怎么做，有两轮工具调用时该怎么做，以及最后什么时候该输出自然语言总结。

3. 增加了有效训练样本数量

- 原始一条多轮对话，会被拆成多条更短的新样本，从而显著扩大训练集规模。

#### 小结

这是一个比较典型的“从数据分布下手，而不是先瞎调参”的方案。  
它的价值不在于“创造”终结信号，而在于**放大和强化原本已经存在的正确信号**。

### 1.3 第二次问题分析

在做完上面的拆分与加权之后，新的问题出现了：

- 模型不再无限循环生成 `<tool_call>` 标签本身；
- 但它会不断重复某个字段或某个关键词，例如截图中的 `fire safety requirements`，最后依旧卡死。

这说明问题并没有真正消失，只是从“不会结束”变成了“被严重带偏，陷入病态生成模式”。

#### 核心原因分析：为什么增强后依然失败

1. Tokenization 和模板陷阱（最可疑）

- 现象：模型可能学会了“开始” `<tool_call>`，却没有真正学会“结束” `<tool_call>` 或后续停止边界。
- 深层原因：
  - `<tool_call>` 这种字符串进入 tokenizer 后，可能会被切成多个 token。
  - 如果训练集中高频出现的是“开头”，而闭合、结束、后续边界信号不稳定，那么模型就容易沿着“继续展开工具调用结构”的路径滑下去。
- 如何处理：
  - 用 `inspect_qwen_dataset.py` 检查一个多轮 `tool_call` 样本在模板化和 token 化之后的 token 字符串。
  - 重点看 `<tool_call>` 的起止边界、`tool_calls` 内容结尾，以及后面紧跟的是用户或系统提示起始 token，还是对话结束 token。

2. 数据集本身的“毒丸”样本（Data Poisoning）

- 现象：2000 条数据里，只要混入了个别格式错误样本，影响就可能被放大。
- 深层原因：模型对训练错误很敏感。只要有一个样本让它学会“tool_call 可以不结束”，就可能把这种坏模式当成高频范式。
- 如何处理：
  - 编写验证脚本，而不是人工肉眼翻。
  - 对每个样本中的每个 `assistant` 回复，检查其 `content` 或 `tool_calls` 字段是否结构完整。
  - 对含有 `tool_calls` 的样本，程序化验证 JSON 是否可被 `json.loads` 正确解析、标签是否成对闭合。
  - 发现问题后，删除或重写这些“毒丸”样本。

3. 对 Tool Call 语法的过度拟合（Overfitting on Syntax）

- 现象：模型对 `<tool_call> ... </tool_call>` 这套语法产生了极强路径依赖。
- 深层原因：相比生成自由文本，工具调用的语法结构更固定、更规整，训练时更容易形成“舒适区”。
- 如何处理：
  - 继续增加**完全不包含任何 `tool_call`** 的样本比例。
  - 增加“纯聊天”与“直接问答”样本，稀释模型对工具调用语法的路径依赖。
  - 目标是让数据集里“不调用工具的样本数量”和“调用工具的样本数量”重新达到一个更平衡的比例。

4. 推理阶段的停止条件不足（Inference-time Issue）

- 现象：模型内部其实可能已经“想停了”，但推理代码没有外部把它刹住。
- 深层原因：`model.generate()` 的 `eos_token_id` 可能只覆盖了一般情况，没有包含工具调用场景里的隐式停止信号。
- 如何处理：
  - 除了 `eos_token_id`，还要显式加入 `stop_strings`。
  - 在 Qwen 模板里，每轮对话通常以 `<|im_end|>` 结束，这应该作为重要停止信号之一。

### 1.4 重复性问题处理：`repetition_penalty` 和 `no_repeat_ngram_size` 到底行不行

这部分的关键观点非常明确：

> 在做真正的数据修复之前，可以先把这两个参数当成**诊断工具**去试；  
> 但它们不是根治方案，甚至很容易把问题改造成更糟的新问题。

#### 1. `repetition_penalty`（重复惩罚）

- 工作原理：降低模型对“最近已经出现过”的 token 的生成概率。
- 可能的正面效果：在生成出第一个 `<tool_call>` 后，组成这个字符串的 token 在下一轮会被压低概率，可能短暂打断死循环。
- 局限：
  - 如果模型已经非常坚定地下一个动作必须继续生成 `<tool_call>`，这个惩罚往往治标不治本。
  - 模型会绕路：插入空格、换行、其他分隔符，再继续发起新的工具调用。

#### 2. `no_repeat_ngram_size`（禁止重复 n 元组）

- 工作原理：禁止模型再次生成一个已经出现过的、长度为 `n` 的 token 序列。
- 可能的正面效果：如果恰好覆盖住 `<tool_call>` 对应的 token 片段，可能阻止它原样复读。
- 局限：
  - 规则太硬，容易误伤正常文本。
  - 也非常容易被规避：模型只需要在两次重复之间插入一个无关 token，就能绕开这个约束。

#### 一个直观例子

- 原始循环：`<tool_call> ... <tool_call> ...`
- 被规避后的循环：`<tool_call> ... **<|im_end|><|im_start|>assistant** <tool_call> ...`

也就是说，**n-gram 已经不同了，但病根没有变**。

### 1.5 JSON 格式词问题与 `tool_list` 是否传入问题探讨

在尝试过上面的解码参数之后，课程里观察到三个更严重的问题：

1. 工具调用开始不准确
2. JSON 格式开始出问题（如英文引号变中文引号、缺引号、缺括号）
3. 稍微扰动一下提示词，结果就显著变差

#### 关于 `tool_list`

这一段的分析是：  
很多时候所谓“传入 `tool_list`”，本质上并不是多了一个独立神奇参数，而是把工具 JSON 规范**注入进了系统提示词的一部分**。

也就是说：

- 你看到的 `tool_list` JSON 文件，最后常常会被拼进 prompt；
- 模型读到的是一段更长、更强约束的系统提示；
- 这会进一步强化模型对固定语法、固定结构的依赖。

但结论同样很明确：

> 无论传入 `tool_list` 还是不传入，都没有真正解决上面的核心问题。

### 1.6 最后的问题定位

这一段是整节课最重要的收束：

> `repetition_penalty` 和 `no_repeat_ngram_size` 这个 Bug 非常典型，  
> 它完整揭示了为什么这些解码参数是“创可贴”，而不是“解药”。

#### 根本矛盾

这里存在一个深层冲突：

- 一边是**微调后的模型权重**，它已经被训练成了一个“严格遵守某种 JSON 与函数调用规范的专家”；
- 另一边是**解码参数**，它又在强制说：“刚才那套词别再用了，换种说法。”

于是模型会崩：

- JSON 需要重复的就是那些引号、冒号、花括号；
- 函数调用名 `name`、`arguments` 以及具体工具名，本来就高度固定；
- 你一边要求它稳定复现固定结构，一边又通过惩罚参数阻止它复现这些关键 token。

#### 这会怎样体现在三个问题上

1. 为什么工具调用开始不准确？

- 当模型本来要生成 `recall_inspection_article` 时，如果 `recall` 之类的 token 因重复惩罚被压低，它就会去找别的“看起来相近”的词，结果函数名错掉。

2. 为什么 JSON 格式开始出问题？

- JSON 的生命线就是重复的引号 `"`、冒号 `:`、花括号 `{}`。
- 一旦这些 token 被惩罚，模型就会去找“不重复”的替代品，于是可能输出中文引号、漏引号，甚至直接漏掉括号。

3. 为什么稍微扰动提示词，结果就变差？

- 微调本质是在概率空间里为某个任务挖出一条很深很窄的“沟道”。
- 当你再加一堆惩罚参数，就像在这条沟道里塞满路障。只要输入稍有变化，模型就更容易脱轨。

### 1.7 总结与最终行动方案

课程最终给出的行动方案非常清晰：

#### 1. 立刻行动：彻底放弃“创可贴”

- 将 `repetition_penalty` **设置回 `1.0`（不生效）**
- 将 `no_repeat_ngram_size` **设置回 `0`（不生效）**

这一步不是“问题解决了”，而是为了**把真正的病因重新暴露出来**。

#### 2. 回归根本：执行真正的“治本”方案

- 数据清洗：编写脚本，验证并清理所有 `tool_calls` 结构不完整的“毒丸”样本。
- 数据平衡：在清洗后的数据上，大量增加“不调用工具”的样本（直接问答、拒绝回答等），稀释模型对工具调用语法的路径依赖。

#### 3. 正确止损：实现无损“安全网”

- 在重新训练模型之后，不要再硬碰 `repetition_penalty`。
- 在推理代码中，正确做法是使用**强制停止条件**，例如：
  - `eos_token_id`
  - `stop_strings`
  - 自定义 `stopping_criteria`
- 当模型生成到对话结束标记（例如 Qwen 的 `<|im_end|>`）时，就立即停止生成。  
  这不会扰乱模型内部的概率分布，只是一个外部刹车。

---

## 2. 评估

### 2.1 快速评估

这一部分截图展示的是 `test_qwen_infer.py` 的开头，用于快速验证模型在典型业务流上的表现。

#### 工作流 1：旅行规划

截图里可见的测试样例如下，后半段被截断，因此这里只保留可见部分：

```python
messages = [
    {
        "role": "system",
        "content": "## 用户信息\n- 用户名：许飞\n- 当前城市ID：101220601\n- 出发日期：2025-09-16\n- 起点坐标：116.794664,33.971707\n\n请在处理用户请求时考虑这些信息，例如：\n- 问路时如果没有明确起点，使用起点坐标116.794664,33.971707\n- 旅行规划时根据用户出发日期范围提供建议\n- 天气查询时根据旅行攻略中的天数来确定查询天数，使用时间段查询\n- 路线查询时起点优先使用起点坐标\n\n你是一个专业的旅行助手，严格按照以下工作流程处理用户请求：\n\n## 工作流1：旅行规划\n..."
    },
    {
        "role": "user",
        "content": "我要去嘉兴度假，求推荐"
    },
    {
        "role": "assistant",
        "content": "",
        "tool_calls": [
            ...
        ]
    }
]
```

从截图能看出的重点是：

- 评估不是只看单轮问答，而是直接按真实工作流去测。
- `system` 提示词里已经把用户信息、默认坐标、天气查询规则、工具调用顺序等都写死到了评估输入中。
- 这个快速评估阶段，本质上是在验证：**模型能不能在真实 prompt 约束下稳定走完完整业务链路**。

##### 训练前

训练前的现象很典型：模型虽然大致知道用户想做“旅行规划”，但经常抓不住真正的任务完成边界。

从截图可见，它会出现几类问题：

- 明明用户只是想做规划，却被带偏到“酒店推荐”之类的邻近任务。
- 明明已有默认起点、出发日期等系统信息，却还是机械追问。
- 对“目的地 + 攻略 + 天气”的双工具链路理解不稳，容易只做一半。

换句话说，训练前模型更像是“知道工具存在”，但**还不会严格按照工作流 1 的流程把任务走完**。

##### 训练后

训练后的样例已经能比较稳定地完成如下链路：

1. 识别用户想做旅行规划。
2. 并行或顺序触发：
   - `search_travel_guide`
   - `get_weather_info`
3. 汇总攻略内容与天气信息。
4. 输出一份结构化的 5 日行程建议。

截图中的嘉兴案例里，模型输出了完整的 `tool_calls`：

```json
[
  {
    "id": "call_710eefea",
    "type": "function",
    "function": {
      "name": "search_travel_guide",
      "arguments": "{\"location\": \"嘉兴\", \"search_mode\": \"hybrid\"}"
    }
  },
  {
    "id": "call_28a4d191",
    "type": "function",
    "function": {
      "name": "get_weather_info",
      "arguments": "{\"location\": \"嘉兴\", \"num_days\": 5, \"start_date\": \"2025-09-16\"}"
    }
  }
]
```

随后，工具返回了两类关键信息：

- 嘉兴 5 日旅游攻略：城市简介、交通、住宿、南湖/乌镇/西塘/盐官/梅花洲等路线安排、美食、购物和预算建议。
- 2025-09-16 起 5 天的天气：晴、小雨、阴/多云、多云等。

模型最后生成了一份完整的“嘉兴 5 日度假旅行计划”，能把天气与景点安排对齐，比如：

- 晴热天优先安排室外或核心景点；
- 小雨天减少户外暴晒，适当改成古镇、街区、室内或轻户外活动；
- 最后再补上穿衣、防晒、门票和预算提醒。

这说明它已经不只是会调工具，而是开始具备**把多工具结果组织成最终可用答案**的能力。

##### 补全类样本

这批图里还给出了一组“信息不完整”的补全类例子：

```text
user: 想出去玩
assistant: 请问您要去哪边的城市旅行呢？或者附近有什么好玩的地方吗？
user: 去嘉兴
assistant: 继续触发 search_travel_guide + get_weather_info
```

这个例子很重要，因为它展示了模型已经学会：

- 信息不足时先追问；
- 信息补全后再继续调用工具；
- 不会在第一轮就盲目乱调。

#### 工作流 2：问路

这一组截图测试的是：当用户问“我要去火车站怎么走”时，模型能不能正确利用系统中的默认起点坐标，而不是继续反复追问起点。

##### 训练前

训练前模型常见问题是：

- 明明系统里已经有默认起点坐标，还是继续要求用户补充起点。
- 虽然识别到是“路线查询”，但对 `query_route` 的调用边界不稳。
- 有时会输出解释性文字，却不真正进入工具查询。

##### 训练后

训练后的表现明显更符合工作流 2。

截图中用户输入：

```text
我要去火车站怎么走
```

模型直接调用：

```json
{
  "name": "query_route",
  "arguments": {
    "start_location": "119.921786,28.451993",
    "end_location": "火车站"
  }
}
```

工具返回了多种出行方式，包括：

- 步行：暂无路线信息
- 公交：总时间约 147 分钟，票价约 2 元，含换乘信息
- 驾车/打车：总时间约 1164 分钟，总距离约 1790867 米，费用约 972 元

模型随后把这些结果整理成自然语言答案，能够：

- 先给出建议的主路线；
- 再把公交、驾车等备选方式补全；
- 最后反问用户是否需要更多帮助。

这说明它已经学会了**优先使用默认起点坐标**，并能把 `tool_response` 进一步转成用户可消费的路线说明。

#### 工作流 3：酒店

这一部分评估的是多工具串联最明显的酒店工作流。

用户样例：

```text
想在深圳找个酒店，价位500-800元左右
```

##### 训练前

训练前的典型问题有两种：

- 要么不稳定地继续追问，不能顺利进入酒店推荐流程。
- 要么只会调用 `recommend_hotels`，但不会继续拿评价，最终回复信息不完整。

##### 训练后

训练后的链路比较清晰，截图中能看到完整的三段式过程：

1. 先调用酒店推荐：

```json
{
  "name": "recommend_hotels",
  "arguments": "{\"requirements\": \"深圳，价位500-800元\"}"
}
```

工具返回：

```text
酒店1：深圳福田香格里拉大酒店
位置：深圳市福田区益田路4088号
```

2. 再调用酒店评价：

```json
{
  "name": "get_hotel_reviews",
  "arguments": "{\"hotel_name\": \"深圳福田香格里拉大酒店\"}"
}
```

工具返回的用户评价里，截图可见的关键信息包括：

- 评价 1：评分 4/5，酒店位置好、房间干净、服务态度友好、早餐丰富、性价比不错。
- 评价 2：评分 5/5，商务设施齐全、会议室专业、网络稳定、房间安静，适合工作和休息。

3. 最终生成总结性回复：

- 明确推荐酒店名称和位置；
- 说明价位落在 500-800 元区间；
- 把评价摘要压缩成一段更自然的推荐语。

这一组样例恰好证明：模型已经能稳定学会**先推荐，再补评价，最后总结**的完整酒店工作流。

#### 工作流 4：旅行类闲聊

这一组测试更偏“不要滥用工具”。

用户输入：

```text
旅行时应该注意什么？
```

##### 训练前

训练前模型的回答虽然也可能围绕旅行展开，但比较容易出现两个问题：

- 为了展示工具能力，倾向把泛化问题也往工具调用方向拉。
- 回答里信息密度不稳定，容易像“半成品说明”。

##### 训练后

训练后的截图显示，模型可以直接进入自然语言回答，而**不调用工具**。

它会围绕以下几类注意事项展开：

- 提前规划：路线、预算、证件与住宿；
- 安全意识：人身安全、财物安全、天气变化；
- 尊重当地习俗与规则；
- 保持信息灵活更新，比如交通、景区开放、天气与突发情况。

这类样本的重要意义在于：模型不再把“能调工具”当成唯一正确动作，而是开始学会**什么时候该直接回答**。

### 2.2 系统性评估

截图里给出的系统性评估指标一共四类：

1. 路由准确率
2. 工具调用准确率（工具名字、工具参数、工具参数的值）
3. 端到端任务成功率
4. 生成速度

这一组指标很合理，因为它把评估拆成了从“会不会选对工作流”，到“会不会把工具调对”，再到“最终任务是否真的完成”，最后再看推理速度。

#### Performance Comparison

最后一张图给出了一组横向对比数据。我把图里的数值整理成表：

| 模型 | Tool Choice Success Rate | Correctness Rate | Processing Time | Parameter Size |
| --- | ---: | ---: | ---: | ---: |
| Qwen3-235B (API) | 100.0% | 95.7% | 11.55s | 235B |
| Qwen3-4B | 100.0% | 87.1% | 8.27s | 4B |
| Qwen3-0.6B | 81.4% | 52.9% | 4.93s | 0.6B |
| Qwen3-0.6B-LoRA | 98.6% | 87.1% | 5.30s | 0.6B |

#### 这组数据说明了什么

1. LoRA 微调对 0.6B 小模型的提升非常明显

- 工具选择成功率：`81.4% -> 98.6%`
- 正确率：`52.9% -> 87.1%`

也就是说，0.6B 原始模型在这个 Function Calling 任务上明显不够稳，但经过 LoRA 之后，已经接近 4B 模型的效果。

2. 成本与效果之间出现了很有意思的平衡点

- `Qwen3-235B (API)` 效果最好，但参数规模和时延都更大。
- `Qwen3-4B` 是一个很强的开源基线。
- `Qwen3-0.6B-LoRA` 在只保留 `0.6B` 参数规模的情况下，把效果抬到了一个很有竞争力的位置。

3. 速度上几乎没怎么牺牲

- `Qwen3-0.6B`：`4.93s`
- `Qwen3-0.6B-LoRA`：`5.30s`

也就是说，LoRA 带来的效果提升非常大，但时延只小幅增加。

#### 小结

从这组系统性评估可以看出，这次微调不是“回答更像样一点”这么简单，而是在真实工作流任务上实现了：

- 更高的路由稳定性
- 更高的工具调用准确率
- 更高的端到端完成率
- 仍然可接受的推理速度

## OCR 不确定处

- 本次补充截图主要覆盖了 `2.1 快速评估` 的后续内容，包括工作流 1 到工作流 4 的训练前/训练后表现，以及 `2.2 系统性评估`。
- 多张图里重复出现了同一份 `system prompt` 长文本，这里只保留了与评估结论直接相关的关键片段，没有逐字重复抄录所有续页。
- `工作流 1` 的嘉兴案例中，攻略正文非常长，本文保留了可见的核心结构、工具调用和结论，没有逐字展开所有景点介绍段落。
- `工作流 4：旅行类闲聊` 的训练前/训练后截图主要展示了终端输出效果，部分原句分辨率有限，因此正文按可辨识要点做了保守重建。
