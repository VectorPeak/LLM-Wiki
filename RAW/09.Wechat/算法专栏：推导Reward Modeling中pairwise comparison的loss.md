---
title: "算法专栏：推导Reward Modeling中pairwise comparison的loss"
source: "https://mp.weixin.qq.com/s/83h9P2Az5xHlVWRBZ2d35A"
author:
  - "[[乔木mq]]"
published:
created: 2026-05-19
description: "在基于提示的奖励建模中，通常采用Bradley-Terry模型对人类偏好进行建模。"
tags:
  - "clippings"
---
乔木mq *2026年4月21日 06:21*

在基于提示的奖励建模中，通常采用Bradley-Terry模型对人类偏好进行建模。下面给出成对比较损失的推导过程，并分析其与直接偏好优化（Direct Preference Optimization, DPO）之间的联系。

给定提示词和两个响应（优选响应）与（劣选响应），目标是训练一个奖励模型，使得优选响应的得分高于劣选响应。

根据Bradley-Terry模型，响应优于的概率由两者奖励分数的Softmax形式给出：

将分子分母同时除以，并利用Sigmoid函数的性质，上式可简化为：

为训练模型，需要最大化观测数据的似然函数，这等价于最小化负对数似然损失。对于单个样本，损失函数定义如下：

代入概率公式，即得到成对逻辑损失：

其中为偏好数据集，为Sigmoid函数。该损失函数的目标是拉大与之间的差距，使尽可能大。

继续分析DPO与上述奖励建模之间的联系。DPO的核心在于：奖励模型与最优策略之间存在显式的数学关系。通过数学变换，DPO将奖励建模损失函数中的“奖励值”替换为“策略概率”，从而绕过训练独立奖励模型的步骤。

在带KL散度约束的强化学习中（基于最大熵强化学习理论），最优策略与奖励函数满足以下关系：

其中为参考模型（通常是经过监督微调的模型），为控制偏离参考模型程度的超参数，为归一化常数。通过移项，可将奖励函数用策略显式表示为：

DPO将上述表达式直接代入Bradley-Terry损失函数。首先计算奖励差值：

其中归一化项在相减时被抵消。将该差值代入，得到DPO的损失函数：

从上述推导可以看出，DPO本质上是Bradley-Terry损失函数在策略空间中的一种重参数化。只要在偏好数据上优化上述DPO损失，就相当于隐式地优化一个Bradley-Terry奖励模型，并同时直接得到对齐后的策略。这使得DPO能够以类似监督学习的简单方式，达到基于人类反馈的强化学习（RLHF）的对齐效果。

算法工程师 · 目录

继续滑动看下一个

乔木mq

向上滑动看下一个