---
title: "算法专栏：50个python常识问题，拿走不谢"
source: "https://mp.weixin.qq.com/s/VrM-qU2CYvw4QMLyKTKurA"
author:
  - "[[乔木mq]]"
published:
created: 2026-05-19
description: "1. Python中如何用一行代码交换两个变量的值？2. 列表（list）和元组（tuple）的主要区别是什么？"
tags:
  - "clippings"
---
乔木mq *2026年4月8日 08:02*

1. 1\. Python中如何用一行代码交换两个变量的值？
2. 2\. 列表（list）和元组（tuple）的主要区别是什么？
3. 3\. `==` 和 `is` 在Python中有什么不同？
4. 4\. 如何检查一个键是否存在于字典中？
5. 5\. 什么是Python中的f-string？请写出一个简单示例。
6. 6\. `range(5)` 会生成哪些数字？
7. 7\. 如何在列表末尾添加一个新元素？
8. 8\. Python中如何注释单行代码？
9. 9\. `len("hello")` 的结果是多少？
10. 10\. 如何将字符串 `"123"` 转换为整数？
11. 11\. 如何创建一个空字典？
12. 12\. `print(type(3.14))` 的输出是什么？
13. 13\. 哪个关键字用于定义函数？
14. 14\. 如何从列表中删除最后一个元素？
15. 15\. `True and False` 的结果是什么？
16. 16\. 如何获取字符串的第一个字符？
17. 17\. Python中哪个符号用于表示取余运算？
18. 18\. 如何判断一个变量是否为 `None` ？
19. 19\. `for i in range(3): print(i)` 会输出什么？
20. 20\. 如何将列表转换为元组？
21. 21\. `input()` 函数返回的数据类型是什么？
22. 22\. 如何在Python中导入模块？
23. 23\. `0 == False` 的结果是？
24. 24\. 如何获取列表的长度？
25. 25\. 字符串 `"Hello".lower()` 的结果是什么？
26. 26\. 如何创建一个空集合（set）？
27. 27\. `5 // 2` 的结果是多少？
28. 28\. `not True` 的结果是什么？
29. 29\. 如何向字典中添加一个键值对？
30. 30\. `list("abc")` 的结果是什么？
31. 31\. `3 ** 2` 等于多少？
32. 32\. 如何反转一个列表？
33. 33\. `bool([])` 的结果是什么？
34. 34\. 如何把整数转换成字符串？
35. 35\. `max([1, 5, 3])` 返回什么？
36. 36\. 如何在字符串中查找子串的位置？
37. 37\. `min("abc")` 的结果是什么？
38. 38\. `float("3.14")` 的结果类型是什么？
39. 39\. `str(100)` 的结果是什么？
40. 40\. 如何复制一个列表？
41. 41\. `abs(-10)` 返回什么？
42. 42\. `round(3.14159, 2)` 的结果是多少？
43. 43\. `chr(65)` 返回什么字符？
44. 44\. `ord('A')` 返回什么数字？
45. 45\. 如何清空一个列表？
46. 46\. `any([False, True, False])` 的结果是什么？
47. 47\. `all([True, True, False])` 的结果是什么？
48. 48\. `sum([1, 2, 3])` 的结果是多少？
49. 49\. 如何判断一个对象是否可迭代？
50. 50\. `sorted([3, 1, 2])` 返回什么？

算法工程师 · 目录

继续滑动看下一个

乔木mq

向上滑动看下一个