---
title: "算法面试专栏：无监督聚类30问"
source: "https://mp.weixin.qq.com/s/H9Ujhc0t-wH5RJwpULUqcg"
author:
  - "[[乔木mq]]"
published:
created: 2026-05-19
description: "1. K-means 的目标函数为 。推导其 Lloyd 算法中簇中心更新  的最优性条件，并证明该更新使目标函数单调下降。"
tags:
  - "clippings"
---
乔木mq *2026年4月1日 07:03*

1. 1\. K-means 的目标函数为 。推导其 Lloyd 算法中簇中心更新 的最优性条件，并证明该更新使目标函数单调下降。
2. 2\. 在高维空间中，K-means 受“维度诅咒”影响显著。推导当维度 时，任意两点间欧氏距离的方差趋近于零的数学形式，并分析其对簇内紧致性判据的破坏机制。
3. 3\. Mini-batch K-means 中，每次仅用小批量更新簇中心。推导其更新规则 的收敛性条件，并分析学习率 的衰减策略对最终解质量的影响。
4. 4\. 谱聚类需计算图拉普拉斯矩阵 的前 个特征向量。若使用 Lanczos 算法近似求解，推导其在稀疏图上的时间复杂度 （ 为边数），并分析数值稳定性对聚类结果的影响。
5. 5\. DBSCAN 的核心点定义依赖 -邻域和 MinPts 参数。推导在非均匀密度数据集中，固定 导致低密度簇被忽略的临界条件，并提出一种基于 k-NN 距离分布的自适应 选择算法。
6. 6\. Gaussian Mixture Model (GMM) 的 EM 算法中，E 步计算后验责任 。推导 M 步中协方差矩阵更新 的梯度形式，并分析奇异协方差的正则化处理。
7. 7\. 在大规模数据下，谱聚类的相似度矩阵 无法全存。设计一种基于 Nyström 近似的低秩展开 ，并推导其对特征向量误差的上界 。
8. 8\. 对比 K-means 与 K-medoids：后者使用实际样本点作为中心。推导 K-medoids 的 PAM 算法中交换代价 的计算复杂度，并分析其在异常值鲁棒性上的优势。
9. 9\. 在分布式环境下执行 K-means，各节点持有局部数据子集。推导一种两阶段聚合策略：先本地聚类再全局合并，并分析通信轮次与最终 SSE（Sum of Squared Errors）的理论关系。
10. 10\. 自编码器用于聚类时（如 DEC），其损失函数包含重构项与 KL 散度项。推导 KL 项 中软分配 对嵌入梯度的调制作用，并解释其如何促使簇分离。
11. 11\. HDBSCAN 通过层次结构提取稳定簇。推导其基于 mutual reachability distance 构建 MST 的过程，并分析 的 k-NN 估计偏差。
12. 12\. 在流式聚类（如 CluStream）中，微簇（micro-cluster）用 表示。推导其合并条件：当两微簇质心距离小于 时触发合并，并分析该阈值与簇球形假设的关系。
13. 13\. 聚类集成（Ensemble Clustering）中，共识矩阵 同 簇 。推导 METIS 或 Spectral 方法在此矩阵上二次聚类的优化目标，并分析基聚类多样性对共识质量的影响。
14. 14\. 在深度聚类中，使用 contrastive loss 增强簇内紧致性。推导 NT-Xent 形式的聚类对比损失：，其中正样本来自同一伪标签簇。
15. 15\. Affinity Propagation 的消息传递更新中，responsibility 和 availability 满足特定递推式。推导其 damping 系数 对收敛速度与振荡现象的调控机制，并分析 时的数值不稳定性。
16. 16\. 在 GPU 上实现 K-means 时，距离计算 可向量化。设计一种基于 cuBLAS 的批量 GEMM 实现：，并分析其显存带宽瓶颈。
17. 17\. 聚类评估指标 Silhouette Coefficient 定义为 。推导其在 样本下的计算复杂度 ，并提出一种基于随机采样的近似算法以降低至 。
18. 18\. 在非凸簇结构数据中，K-means 失效。推导 Kernel K-means 的目标函数 在再生核希尔伯特空间（RKHS）中的等价形式，并分析核选择对簇形状的适应能力。
19. 19\. 在联邦聚类场景中，各客户端本地执行 GMM。推导一种安全聚合协议：服务器仅接收本地充分统计量 ，并分析其在差分隐私噪声注入下的模型偏差。
20. 20\. 使用 t-SNE 进行可视化后再聚类存在陷阱。推导 t-SNE 的梯度 如何扭曲原始空间距离，并证明其低维嵌入不适合直接用于 K-means。
21. 21\. 在在线聚类中，新样本到来需动态调整簇结构。推导一种基于 Dirichlet Process 的无限混合模型更新规则，并分析 concentration parameter 对簇数量增长速率的控制作用。
22. 22\. 聚类中的“空簇”问题在 K-means 中常见。推导一种智能重初始化策略：将最大 SSE 簇分裂，新中心设为 ，其中 为主成分方向。
23. 23\. 在量化感知聚类中，输入特征已被 INT8 量化。推导量化误差 对欧氏距离的扰动上界 （ 为量化步长），并分析其对簇纯度的影响。
24. 24\. 对比 Louvain 与 Leiden 算法在社区发现中的差异：后者引入 refinement phase。推导 Leiden 中分区质量函数（如 modularity）在 refinement 阶段的局部最优条件，并分析其对孤立节点的处理改进。
25. 25\. 在大规模图聚类中，使用 GraphSAGE 生成节点嵌入后再聚类。推导其邻居采样策略对嵌入同质性（homophily）的破坏程度，并提出一种基于重要性采样的修正方案。
26. 26\. 聚类稳定性常通过 bootstrap 评估。推导 Jaccard 系数在两次重采样聚类结果间的期望值 与真实簇结构清晰度的关系，并分析其在噪声数据下的偏差。
27. 27\. 在多视图聚类中，不同特征集 需协同优化。推导共正则化（co-regularization）目标：，其中 为软分配矩阵。
28. 28\. 使用 Wasserstein 距离度量簇间差异（如 in Wassertein K-means）。推导其 Sinkhorn 近似下的可微形式，并分析熵正则化参数 对梯度平滑性与计算开销的权衡。
29. 29\. 在时间序列聚类中，DTW 距离替代欧氏距离。推导 DTW 的动态规划表在 GPU 上的并行填充策略，并分析其 复杂度对长序列的可扩展性限制。
30. 30\. 聚类中的“维度选择”问题：并非所有特征对分组有用。推导一种基于 Fisher Score 的特征加权 K-means 目标函数 ，并分析权重 与类间/类内散度比的关系。

算法工程师 · 目录

继续滑动看下一个

乔木mq

向上滑动看下一个