RAG 的话，其实我们的项目就是一个最好的经典 RAG 的例子。所以建议了解 RAG 的八股、知识后，可以去了解这个项目，这个项目把 RAG 最经典的环节都串起来了，并且这个项目配的模拟面试/项目复习的 SKILL，就是去掌握项目，学习 RAG 各个环节的好方法。该项目部分也会配一些该项目面试的真题，和前面的 RAG 理论相互补充。学好这个理论/项目，经典 RAG 就差不多了

**RAG 进阶是：Graph RAG、Agentic RAG、知识图谱**。这里我笔记总结了基本概念，我的策略是问到我就说会，讲述我的概念，但是我没有深入，因为这是 RAG 进阶技巧，对于那种对 RAG 要求比较高的岗位，专门做 RAG 的组可能会用。如果求职方向就是大模型应用、大模型算法、算法工程，没有指定 RAG 方向的话，没有余力的话，对于 RAG 的进阶部分，可以就只看一下笔记本节 RAG 进阶的概念，了解基本概念就好

## 1. RAG简介
### 1.1 什么是RAG?
RAG(Retrieval-Augmented Generation 检索增强生成) 它的核心是将模型内部学到的“**参数化知识**”（模型权重中固化的、模糊的“记忆”），与来自外部知识库的“**非参数化知识**”（精准、可随时更新的外部数据）相结合. 用以解决大模型的 1.幻觉问题 2.生成信息时效性不足 3.生成信息不专业 4.需要私有化本地部署知识库的场景

其运作逻辑就是在 LLM 生成文本前，先通过检索机制从外部知识库中动态获取相关信息，并将这些“参考资料”融入生成过程，从而解决大模型

### 1.2 RAG vs 微调
在选择具体的技术路径时，一个重要的考量是成本与效益的平衡。通常，我们应优先选择对模型改动最小、成本最低的方案，所以技术选型路径往往遵循的顺序是

**提示词工程（Prompt Engineering） -> 检索增强生成 -> 微调（Fine-tuning）**

我们可以从两个维度来理解这些技术的区别。如上图所示，**横轴代表“LLM 优化”**，即对模型本身进行多大程度的修改。从左到右，优化的程度越来越深，其中提示工程和 RAG 完全不改变模型权重，而微调则直接修改模型参数。**纵轴代表“上下文优化”**，是对输入给模型的信息进行多大程度的增强。从下到上，增强的程度越来越高，其中提示工程只是优化提问方式，而 RAG 则通过引入外部知识库，极大地丰富了上下文信息


基于此，我们的选择路径就清晰了：
- **先尝试提示工程**：通过精心设计提示词来引导模型，适用于任务简单、模型已有相关知识的场景
- **再选择 RAG**：如果模型缺乏特定或实时知识而无法回答，则使用 RAG，通过外挂知识库为其提供上下文信息
- **最后考虑微调**：当目标是改变模型“如何做”（行为/风格/格式）而不是“知道什么”（知识）时，微调是最终且最合适的选择。例如，让模型学会严格遵循某种独特的输出格式、模仿特定人物的对话风格，或者将极其复杂的指令“蒸馏”进模型权重中


### 1.3 RAG评价指标
评估是非常关键的, 因为它回答了RAG开发与应用中的一系列问题. 如何量化地追踪、迭代并提升RAG应用的性能？面对两个不同的RAG应用，如何客观地评判孰优孰劣？

在 RAG 系统中，较常见的一套评估方法是**RAG三元组(RAG Triad)**。它主要从三个维度判断生成结果的质量：**忠实度、答案相关性和上下文相关性**。这套方法的优势在于，大部分指标不需要参考答案. 并在**RAGAS/TruLens**等工具中有深入的应用
![[Pasted image 20260422205050.png]]

#### 1.3.1 RAG三元组

#### 1.3.2 分层评估



~~~
*** Q1:如何评价RAG系统的效果?

A:
1.先说评估维度:三大核心维度 忠实度,答案相关性,上下文相关性
(要了解三大指标的公式 + RAGAS的三个指标不需要参考答案)

2.方法论:分层测+诊断矩阵 Retriever -> Generater -> 端到端
在评测的时候也有个顺序,要确定到底是检索的效果不好,还是生成的效果不好,所以要分层测.准备个黄金数据集(20-50条) 先测Retriever(Precision+Recall+MRR+NDCG), 再测Generater(给完美上下文/召回文本,测Faithfullness+Answer Relevance), 最后再端到端地测一遍(诊断矩阵) + 黄金数据集:20-50条

3.LLM-as-Judge 局限 + 缓解
局限:我读过一篇论文,大模型自己在评分的时候呢,实际上是有点局限的. 比如说位置偏差,冗长偏差,自我/家族偏好
缓解:怎么缓解呢,换模型家族/多裁判, 微调小模型, 人机混合(大部分自动,高风险的交给人工)

4.评测的工具选择
行业默认是RAGAS. 当然也可以用DeepEval,这个支持CI/CD集成

5.三层叠加方案
大部分情况下/想做个快速原型验证 直接用RAGAS打分就行了. 如果是追求真正靠谱的方案呢,可以用三层叠加的方案. 第一层用RAGAS/DeepEval做初筛. 第二层积累领域的定制测试集,把真实的失败case不断加进去,这才是团队最宝贵的资产. 第三层就是人工抽检,高风险的系统不可省略
~~~


~~~
(扩展)Q2:为什么不能用传统的NLP指标来评价RAG? 比如说BLEU,ROUGE

A:
1.不懂语义,只算字面重叠,不关注上下文,没法检查幻觉
BLEU和ROUGE是做机器翻译和文本摘要使用的指标.
~~~


~~~
(扩展)Q3:RAG的七种失败模式,你了解过吗?

A:
检索侧:1.内容缺失 2.排名太低 3.上下文截断
生成侧:1.提取失败 2.格式错误 3.粒度不当 4.回答不完整
~~~
#### 1.3.1RAGAS
- 忠实度(Faithfulness):
- 答案相关性(Answer Relevancy):
- 上下文相关性(Context Relevance):
	




## 常见向量数据库
- 常见的向量数据库有Milvus,Pinecone,Weaviate,Qdrant,Chroma 具体来讲:
**Milvus:**
	**Milvus是一款重型, 分布式, 面向大规模数据的向量数据库**, 

**Pinecone:**

**Weaviate:** 
	**Weaviate是一款对象化、云原生和可扩展的向量数据库.** 具有鲁棒性，云原生性和可扩展性的特点。它结合了向量搜索和结构化过滤的能力，同时拥有云原生数据库的容错性和可伸缩性。在百万级的向量数据上，可以实现毫秒级的检索。它可以方便调用 OpenAI、Cohere 以及 Hugging Face 平台提供的先进的数据向量化接口和模型。缺点是：目前只支持 HNSW 这一种检索算法。

**Chroma:****
	**Chroma是一款轻量级、用于快速生成原型验证、开发者友好的向量数据库.** 是一款 AI 原生的开源向量数据库，主要功能和特点围绕 AI 应用开发而设计。主要针对大模型 RAG 场景，目前只实现了单机方案。相比 Weaviate 和 Milvus，Chroma 更轻量化。检索上实现了 FAISS 索引，不支持 GPU 加速。





| 名称       | 类型定位          | 索引/检索特点                                    | 优点                          | 局限                          | 适用场景                 | 规模倾向      |
| -------- | ------------- | ------------------------------------------ | --------------------------- | --------------------------- | -------------------- | --------- |
| Milvus   | 开源分布式向量数据库    | 支持多种索引与混合检索，强调分布式与大规模                      | 高性能、可分片、可持久化、K8s 友好、适合企业级部署 | 部署和运维相对复杂                   | 大规模 RAG、推荐、图像/多模态检索  | 超大规模、生产级  |
| Pinecone | 托管型向量数据库服务    | 官方对底层实现做抽象，对外提供 dense / sparse 检索、过滤、重排等能力 | 免运维、上线快、弹性扩缩容、适合生产环境        | 成本较高、底层可控性较弱、平台锁定更强         | 企业级 RAG、在线搜索、多租户服务   | 生产级大规模    |
| Weaviate | 开源向量数据库       | 向量检索和结构化属性结合紧密，支持过滤与模块化集成                  | 对象模型清晰，AI 应用开发体验好，适合知识库     | 资源占用通常高于轻量方案                | 知识库、语义搜索、问答系统        | 中大型、生产级   |
| Chroma   | AI 应用导向的向量数据库 | 强调开发者友好，支持 dense / sparse / hybrid 检索      | 上手快、API 简单、适合原型验证和本地开发      | 超大规模、多租户、复杂运维场景下通常不是第一选择    | 本地 RAG、Agent 原型、实验项目 | 小到中等规模    |
| Qdrant   | 开源向量数据库       | 以 HNSW 和 payload 过滤能力见长，工程体验均衡             | 过滤强、检索稳定、部署相对直接，RAG 场景常用    | 生态体量通常不如最头部几家综合平台           | 语义搜索、企业知识库、推荐系统      | 中大型、生产级   |
| FAISS    | 向量相似度搜索库      | 提供高性能 ANN / 相似度搜索与聚类，支持多种索引结构和 GPU         | 检索性能强，适合自定义系统和研究            | 不是完整数据库，不负责分布式、高可用、事务、持久化治理 | 自研检索引擎、本地实验、算法研究     | 取决于外部系统封装 |


## 2. RAG的重要步骤 (最小可行性系统MVP)
**1.数据准备与清洗:** 这是系统的地基(GIGO) 需要将PDF,Word等多源异构数据进行标准化，并采用合理的**分块策略**（如按语义段落切分而非固定字符数），避免信息在切割中支离破碎
**2.构建索引:** 将切分好的文本通过**嵌入模型**转换为向量,并存入向量数据库.可以在此阶段关联**元数据**（如来源、页码），这对后续的精确引用很有帮助
**3.语义召回与检索:**
**4.生成与提示词工程:** 最后,设计一套清晰的Prompt模板, 引导LLM基于检索到的上下文去回答用户问题

### 2.1 
### 2.1 chunking(分块):
“分块”是非常高频的一个考点，一定要掌握。常见问题包括：介绍一下 RAG 中常见的分割策略（2.1）、如何避免语义被切掉（2.2）、你的项目中是如何使用分块策略的，为什么（2.4）。
#### 2.1.1 chunking策略:
1. **固定长度切割-Fixed-size Chunking**:
	按固定 token/字符数切，简单但容易把语义切断
	
2. **按段落切割-Paragraph Chunking** 
    按自然段切，语义更完整，但块大小不均
    
3. **滑动窗口切割-Sliding-window Chunking**  
    相邻 chunk 有重叠，保留上下文，减少语义断裂，但更费算力和存储
    
4. **语义切割-Semantic Chunking**  
    按语义边界智能切，最贴合内容，但实现更复杂，例如借助 NLTK 或一些深度学习模型
    
5. **结构化切割-Structure-aware Chunking**
    按标题、列表、章节结构切，适合技术文档，报告等文档型内容
    
6. **递归字符分块器-Recursive Character Chunking**  
    按分隔符优先级递归切分，是工程里常用的折中方案。这个方法可以看作综合了固定长度、滑动窗口、结构化切割等思路
	
	**辅助记忆:FPSSSR**
    
#### 2.1.2 如何避免语义被chunk?
在滑动窗口方法中，切割的相邻两块之间都会有一些重叠。按照语义切割的方法，天然也会想让语义表达一个意思的块来切割到一个 chunk 中，这些方法会缓解语义被切掉的问题

同时，也可以使用一些对切割后的结构进行评估方法，比如使用模型来判断语义是否完整等方法来进行评估，对于语义丢失严重的切块重新分割
#### 2.1.3你的项目是如何chunk的?
- 这个问题希望大家能够自己思考，其实是要理解了上面的问题，然后结合自己的项目来回答，言之有理即可，我来给大家展示一下我结合自己的项目的回答）

- 在按摩房预约系统中，我根据不同的场景使用了不同的分块策略

**第一类：针对技师列表、服务菜单等强结构化数据**  
我采用了“不分块”的策略。

- **具体做法**：将每一个技师的完整介绍（包括姓名、技能、工龄、评价）作为一个独立的 Document 存入向量库，不做任何切分。
- **这么做的原因**：技师信息是一个不可分割的语义实体。如果强行按长度切分，很容易导致“张三擅长……”比如检索到了“擅长针灸”这个片段，但却丢失了“张三”这个名字的上下文。保留整体能确保 LLM 获取到完整的实体画像，保证推荐的准确性。

**第二类：针对用户协议、门店介绍、退改规则等长文本非结构化数据**  
我使用了递归字符分块策略。

- **具体做法**：利用 LangChain 提供的标准工具 RecursiveCharacterTextSplitter 进行处理。我通常将 chunk_size 设定在 400-500 tokens 左右，并设置 50 tokens 的 chunk_overlap（重叠）。
    
- **这么做的原因**：
    
    a. **语义完整性**：这种方法不是生硬地按字符数切割，而是优先尝试按段落（\n\n）切割，其次按句子（\n 或 。）切割。这最大程度地保留了文本的自然语义逻辑。
    
    b. **上下文连贯**：通过设置重叠区域（Overlap），解决了“滑动窗口”的问题，防止关键信息（如电话号码）刚好被切断在两个块中间，导致检索失效。
    
    c. **适配模型**：切分后的长度非常适合 Embedding 模型的输入窗口，也避免了将数千字的长文直接塞给 LLM 导致 “Lost in the Middle” 现象或 Token 浪费。

### 2.2 召回
#### 2.2.1 召回的常用策略:

RAG 系统可以使用 **粗排 + 重排** 两个阶段来完成召回。粗排通常使用**稠密向量检索模型**来做语义匹配，召回语义相关文档。当然也可以再结合**稀疏向量检索模型**来补充使用，因为稀疏向量检索模型更擅长捕捉关键信息来提高召回效果

粗排后，可以使用**交叉编码器**，比如 BERT，来对候选文档进行**深入语义排序**，提高召回质量

#### 2.2.2 稠密向量以及稀疏向量

参考书籍《大模型 RAG 实战》第二章文本召回模型

RAG 系统中的文本向量检索模型大致分成两类：一类是基于 **BERT、GPT 等深度学习模型** 的**稠密向量检索模型**，另一类是以 **TF-IDF、BM25** 为代表的**稀疏向量检索模型**

 **稠密向量检索模型更擅长提取文本中的语义信息，稀疏向量检索模型更擅长提取关键信息**

#### 2.2.3 对称检索和非对称检索
参考书籍《大模型 RAG 实战》第三章文本召回模型

*对称检索**是找到相似的句子，与向量检索基于计算向量相似度的原理天然匹配，只需要模型具有较强的内容抽象能力即可。

**非对称搜索**是根据问题召回答案，要求模型能够将问题和答案映射到同一空间，而问题和答案通常具有不同的文本结构。并非所有的向量化模型都支持非对称检索，只有经过大量问答数据训练的模型才能提供支持。


### 2.3 召回优化策略:
#### 2.3.1 短文本全局信息增强

 因为切块的过程难免会造成全局文本信息的丢失，因此我们为切块后的短文本补充其所属的长文本信息

比如论文《DAPR: A Benchmark on Document-Aware Passage Retrieval》中提到，我们可以将**长文本的前三句话、长文本的标题、长文本的关键词**作为全局信息，和切块后的文本一起保存

#### 2.3.2 召回内容上下文扩充

在 RAG 切块的时候，对长文本切块后，可能会有较大的信息损失，因为我们从数据库中召回的通常是短文本。这时我们可以采用召回内容上下文补充的方法：**召回的是短文本，但是输入 LLM 处理的是短文本的父文本（长文本）**。好处是**既精准（基于短文本匹配），又完整（基于长文本输入）**。

-在 LangChain 中，我们可以使用 **ParentDocumentRetriever** 来完成这一功能，它使用了两个分割器，**Parent_splitter** 和 **child_splitter**。前者用于确定在 RAG 场景下输入到大模型的父文本块的大小，后者确定从向量数据库中召回的子文本块的大小。同时还会存储父子文本块的映射关系。当使用 get_relevant_documents 召回时，返回的实际上是父文本的内容。

#### 2.3.3 文本多向量表示

在构建向量数据库的时候，可以使用多种向量来表示同一段文本。比如有一段文本 A，我们不仅可以使用完整的 A 来生成向量，还可以使用它的**子集、总结句**来生成补充向量。当召回任意的补充向量对应的文本后，并不是将其输入大模型中，而是将其对应的文本 A 输入。

常见的生成补充向量的方法有：

- **文本切块**：将原始文本切块后保存到数据库中，可以在召回时增强对更细粒度的表示。
- **文章摘要**：利用 LLM 生成摘要。
- **假设性回答**：可以利用大模型的能力，根据文本提出问题，把问题作为补充信息。这种方法的思想是用问题召回问题会比用问题召回答案更容易。

#### 2.3.4 内容查询优化

很多情况下，用户提出的内容有口语化、语义模糊、无关内容多的问题。因此直接将问题变成向量可能和用户的真实意图不相关，从而影响 RAG 场景下的召回效果。因此，我们可以对用户的原始问题进行改写和扩充，再进行召回。

常见的对原始问题优化的方法有：

1. 使用 LLM 改写和扩充，比如 LangChain 提供的 MultiQueryRetriever 就能集成了这个功能。
2. 对历史问题进行总结，然后再进行召回，这个特别适用于多轮对话中。比如：

用户：中国的首都在哪里？  
LLM：北京

用户：那里有哪些好玩的景点？

这时直接用原来的问题召回显然太模糊，如果用 LLM 总结后，问题就变成了：北京有哪些好玩的景点。

#### 2.3.5 召回文本重排序

在 RAG 场景下，默认使用的是向量召回，检索效率高。但是由于文本向量化过程中存在着信息损失，并且在召回过程无法考虑查询用户和候选文本上下文关系，因此召回精度不高。所以在召回向量后，通常还会进行召回文本的重排序的精筛。

最常用的重排序模型是基于 **Transformer 编码器架构的交叉编码器**。将用户查询和一段候选文本同时输入到模型之中，预测它们之间的相关性分布。使用这种方法可以考虑到用户查询和候选文本的上下文关系，因此有更高的精度。

如果用户愿意，我也可以继续把 4.3 整体整理成一版“召回优化策略总表”。


#### 2.3.6 结合元数据召回

在实际场景中，很多情况下我们不需要在整个向量数据库中进行召回。如果在存储文本向量的时候同时记录了每个向量相关的标签，就可以在召回时现根据标签信息过滤出所需的向量子集，有利于提高检索速率，避免检索无用的信息。

现在的大模型应用框架，LangChain 就基于多种向量数据库，集成了元数据写入和召回过滤的能力。我们可以使用 LangChain 的 Create_metadata_tagger 的接口，借助大模型的能力，自动生成元数据。

如果用户愿意，我也可以顺手帮你把这段再润色一下。

## 3.词嵌入
### 3.1 常见的文本嵌入的方式

|模型|架构|维度|训练目标|优点|缺点|典型应用场景|
|---|---|---|---|---|---|---|
|BERT|Transformer|768|Masked LM + NSP|强大的语言理解能力|需要池化处理，检索性能一般|NLP 下游任务，如分类、问答|
|SBERT|Transformer|768|句子相似度优化|适合语义检索，开源易用|推理速度较慢|语义搜索、RAG|
|OpenAI Embeddings|Transformer|1536|专门针对 embedding 优化|高精度，开箱即用，支持多语言|需 API 调用，成本较高|语义检索、推荐、RAG|
|E5|Transformer|768-1024|检索任务优化|检索性能强，开源|需要部署，资源消耗大|搜索、问答、RAG|



## 4.RAG评估
### 4.1 RAG评价指标(面试必问)
评估是非常关键的, 因为它回答了RAG开发与应用中的一系列问题. 如何量化地追踪、迭代并提升RAG应用的性能？面对两个不同的RAG应用，如何客观地评判孰优孰劣？

在 RAG 系统中，较常见的一套评估方法是**RAG三元组(RAG Triad)**。它主要从三个维度判断生成结果的质量：**忠实度、答案相关性和上下文相关性**。这套方法的优势在于，大部分指标不需要参考答案. 并在**RAGAS/TruLens**等工具中有深入的应用
![[Pasted image 20260422205050.png]]

#### 4.1.1 RAG三元组
**忠实度(Faithfullness):**
	**核心问题:** RAG系统的回答是否脱离了材料?
	**重要性:**    这个维度主要在于量化LLM的“幻觉”程度。一个高忠实度的回答意味着模型严格遵守了上下文，没有捏造或歪曲事实
	**计算公式:**$$
\text{Faithfulness}
=
\frac{\#\text{可由上下文支持的陈述}}{\#\text{回答中的总陈述}}
$$
---
**答案相关性(Answer Relevance):**
	**核心问题:** 回答是否真正回应了用户问题？
	**重要性:** 用于衡量是否“答对题意”，避免答非所问、遗漏重点或夹带无关内容
	**计算公式:**$$\text{Answer Relevance}
=
\frac{1}{N}\sum_{i=1}^{N}\cos(E_{g_i}, E_q)
$$
	其中，$E_q$ 是原问题的向量表示，$E_{g_i}$ 是根据回答反向生成的第 $i$ 个问题的向量表示。

---
**上下文相关性(Context Relevance):**
	**核心问题:** 检索到的上下文是否与用户问题相关？
	**重要性:** 用于衡量检索阶段的质量。若上下文本身偏题，后续生成结果通常也会受影响
	**计算公式:** $$\text{Context Relevance}=\frac{1}{2}\left(\frac{s_1}{2}+\frac{s_2}{2}\right)=\frac{s_1+s_2}{4}, \quad s_1,s_2 \in \{0,1,2\}$$
	其中，$s_1,s_2$ 为 LLM 对上下文相关性的两次自行独立评分；分数越接近 1，表示上下文越相关
	
#### 4.1.2 分层评估
##### 4.1.2.1 **评估Retriever**
评估Retriever 聚焦于RAG三元组中的 上下文相关性 (Context Relevance)**，本质上是一次**白盒测试。此阶段的评估需要一个标注数据集，其中包含一系列查询以及每个查询对应的真实相关文档

**上下文精确率 (Context Precision):** 衡量检索结果的准确性。计算在检索到的前 **k** 个文档中相关文档所占的比例，其中 **k** 是一个预设的数字（例如，k=3或k=5），代表评估的范围。高精确率意味着检索结果的噪声较少。
$$\mathrm{Precision}@k=\frac{\text{检索到的前 } k \text{ 个结果中的相关文档数}}{k}$$
**上下文召回率 (Context Recall):** 衡量检索结果的完整性。计算在检索到的前 **k** 个文档中，找到的相关文档占所有真实相关文档总数的比例。高召回率意味着系统能够成功找回大部分关键信息。 
$$\mathrm{Recall}@k=\frac{\text{检索到的前 } k \text{ 个结果中的相关文档数}}{\text{数据集中所有相关文档的总数}}$$
 
 **F1分数 (F1-Score):** F1分数是精确率和召回率的调和平均数，它同时兼顾了这两个指标，在它们之间寻求平衡。当精确率和召回率都高时，F1分数也高。
$$F_1=2 \cdot\frac{\mathrm{Precision} \times \mathrm{Recall}}{\mathrm{Precision} + \mathrm{Recall}}$$
    
**平均倒数排名 (MRR - Mean Reciprocal Rank):** 评估系统将第一个相关文档排在靠前位置的能力。对于一个查询，倒数排名是第一个相关文档排名的倒数。MRR是所有查询的倒数排名的平均值。该指标适用于用户通常只关心第一个正确答案的场景。
$$\mathrm{MRR}=\frac{1}{|Q|}\sum_{q=1}^{|Q|}\frac{1}{\mathrm{rank}_q}$$

**平均准确率均值 (MAP - Mean Average Precision):** MAP是一个综合性指标，同时评估了检索结果的精确率和相关文档的排名。它先计算每个查询的平均精确率（AP），然后对所有查询的AP取平均值。AP本身是基于每个相关文档被检索到时的精确率计算的。
$$\mathrm{MAP}=\frac{1}{|Q|}\sum_{q=1}^{|Q|} AP(q)$$其中，$|Q|$ 是查询总数，$AP(q)$ 是第 $q$ 个查询的平均精确率（Average Precision）
    
---
##### 4.1.2.2 **评估Generator**

**忠实度(Faithfullness):** 同上

**答案相关性(Answer Relevance):** 同上

##### 4.1.2.3 **NLP经典评估指标(基于词汇重叠)**(扩展)
这类指标需要在数据集中包含一个或多个“标准答案”。它们通过计算生成答案与标准答案之间 n-gram（连续的n个词）的重叠程度来评估质量

---
**BLEU**（Bilingual Evaluation Understudy）更侧重于精确率，衡量生成的答案中有多少词是有效的(即在标准答案中出现过). 它还引入了**长度惩罚机制** , 避免模型生成过短的句子, 因此更加适合评估答案的流畅度和准确性
$$\mathrm{BLEU}=BP \times \exp\left(\sum_{n=1}^{N} w_n \log p_n\right)$$
其中：
- $BP$：长度惩罚因子（brevity penalty）
- $p_n$：修正后的 `n-gram` 精确率
- $w_n$：不同阶 `n-gram` 的权重，通常满足 $\sum_{n=1}^{N} w_n = 1$

> 直观上, BLEU可以理解为先分别计算 1-gram、2-gram、3-gram、4-gram 等层面的精确率，再做加权组合，并对过短答案进行惩罚。

---
**ROUGE**（Recall-Oriented Understudy for Gisting Evaluation）关注的是**召回率** 即
标准答案中的内容，有多少被生成答案覆盖到了。因此，ROUGE 常用于评估生成内容的**完整性**，尤其常见于摘要任务。

常用变体包括：
- `ROUGE-N`：统计 `n-gram` 重叠情况
- `ROUGE-L`：基于最长公共子序列（LCS, Longest Common Subsequence）

$$\mathrm{ROUGE\text{-}N}=\frac{C_{\mathrm{match}}(n\text{-gram})}{C_{\mathrm{ref}}(n\text{-gram})}$$
其中：
- $C_{\mathrm{match}}(n\text{-gram})$ 表示生成答案与参考答案中匹配的 `n-gram` 数量
- $C_{\mathrm{ref}}(n\text{-gram})$ 表示参考答案中的 `n-gram` 总数

---
**METEOR**（Metric for Evaluation of Translation with Explicit ORdering）可以看作是对 BLEU 的改进版本.  它不仅考虑**精确率**，还考虑**召回率**，并通过词干匹配、同义词匹配等方式，更好地捕捉文本之间的**语义相似性**。
$$F_{\mathrm{mean}}=\frac{P \times R}{\alpha P + (1-\alpha)R}$$

$$\mathrm{METEOR}=F_{\mathrm{mean}} \times (1-\mathrm{Penalty})$$

其中：
- $P$：精确率（Precision）
- $R$：召回率（Recall）
- $\alpha$：控制精确率与召回率相对权重的参数
- $\mathrm{Penalty}$：基于语序或分块对齐情况的惩罚项

### 4.2 RAG评价工具
#### 4.2.1 RAGAS
行业默认首选, RAGAS（RAG Assessment）是一个**独立的,专注于RAG的开源评估框架** 最大的特色是支持**无参考答案评估**

根据RAGAS的官方文档，一个标准的评估数据集应包含 `question`（问题）,`answer`（RAG系统生成的答案）,`contexts`（检索到的上下文）以及 `ground_truth`（标准参考答案）这四列。不过，`ground_truth` 对于计算 `context_recall` 等指标是必需的，但对于 `faithfulness` 等指标则是可选的

#### 4.2.2 其他评价工具

**LlamaIndex Evaluation:**
	LlamaIndex Evaluation 是 LlamaIndex 内置的 RAG 评估模块，用于在开发过程中快速验证和比较不同策略的效果

### 4.3 RAG评价面试题
~~~
(必问)Q1:如何评价RAG系统的效果?

A:o
1.先说评估维度:三大核心维度 忠实度,答案相关性,上下文相关性
(要了解三大指标的公式 + RAGAS的三个指标不需要参考答案)

2.方法论:分层测+诊断矩阵 Retriever -> Generater -> 端到端
在评测的时候也有个顺序,要确定到底是检索的效果不好,还是生成的效果不好,所以要分层测.准备个黄金数据集(20-50条) 先测Retriever(Precision+Recall+MRR+NDCG), 再测Generator(给完美上下文/召回文本,测Faithfullness+Answer Relevance), 最后再端到端地测一遍(诊断矩阵) + 黄金数据集:20-50条

3.LLM-as-Judge 局限 + 缓解
局限:我读过一篇论文,大模型自己在评分的时候呢,实际上是有点局限的. 比如说位置偏差,冗长偏差,自我/家族偏好
缓解:怎么缓解呢,换模型家族/多裁判, 微调小模型, 人机混合(大部分自动,高风险的交给人工)

4.评测的工具选择
行业默认是RAGAS. 当然也可以用DeepEval,这个支持CI/CD集成

5.三层叠加方案
大部分情况下/想做个快速原型验证 直接用RAGAS打分就行了. 如果是追求真正靠谱的方案呢,可以用三层叠加的方案. 第一层用RAGAS/DeepEval做初筛. 第二层积累领域的定制测试集,把真实的失败case不断加进去,这才是团队最宝贵的资产. 第三层就是人工抽检,高风险的系统不可省略
~~~

~~~
(扩展)Q2:怎么做一个标准的评估数据集?
A:
根据RAGAS的官方文档，一个标准的评估数据集应包含 `question`（问题）、`answer`（RAG系统生成的答案）、`contexts`（检索到的上下文）以及 `ground_truth`（标准参考答案）这四列。不过，`ground_truth` 对于计算 `context_recall` 等指标是必需的，但对于 `faithfulness` 等指标则是可选的
~~~

~~~
(扩展)Q3:为什么不能用传统的NLP指标来评价RAG? 比如说BLEU,ROUGE

A:
1.不懂语义,只算字面重叠,不关注上下文,没法检查幻觉
BLEU和ROUGE是做机器翻译和文本摘要使用的指标.
~~~


~~~
(扩展)Q4:RAG的七种失败模式,你了解过吗?

A:
检索侧:1.内容缺失 2.排名太低 3.上下文截断
生成侧:1.提取失败 2.格式错误 3.粒度不当 4.回答不完整
~~~

## 5. RAG进阶-Graph RAG

此部分为 RAG 进阶部分，如前所述，一般岗位可能不会考察这么深，除非用户自己简历写了 Graph RAG、Agentic RAG，或者本身就是专门做 RAG 的组，可能会考到这里。我的原则是，这部分概念要会，问 RAG 有啥新方向，相关概念答得出来，但没有深入研究
### 5.1 RAG新方向

这俩新方向，虽然可能没深入研究，但是得知道，概念要能讲出来

目前典型的 RAG 的新方向是 **Agentic RAG** 和 **Graph RAG**，具体来讲：

**Agentic RAG** 的核心是把 RAG 从一个固定流水线变成一个有决策能力的 Agent。传统 RAG 就是检索一轮、生成答案，结束了。但很多复杂问题一轮检索根本不够，Agentic RAG 让模型自己判断：一轮结果够不够？不够就再检索一轮，或者换个检索策略。像 **Self-RAG、Corrective RAG** 都是这个思路，本质就是让**检索过程变得更智能，可以自我纠错**

**Graph RAG** 解决的是另一个问题。传统向量检索擅长找语义相似的内容，但很难处理跨文档的关联推理。比如问人物之间的关系，信息散落在十几个段落里，靠 embedding 相似度很难串起来。Graph RAG 的做法是先从文档里抽取实体和关系建成知识图谱，检索时在图上做关系遍历，特别适合需要全局理解的问题。

我觉得这两个方向是互补的：一个解决检索策略的智能化，一个解决知识关联和推理，未来可能会结合起来用

### 5.2 RAG与知识图谱
（知识图谱也能和 RAG 结合使用。但是其实对于可能 80% 的问题，靠高质量的切片和双路召回就能够解决，只有 20% 的问题，需要跨文件全局关联的硬核需求，值得花 200% 的精力去建立图谱。总结来说，知识图谱较为复杂，不是每个企业都会用，成本高。也属于进阶路线。所以如果用户没有学好传统的双路召回 RAG，先不用来学这个结合图谱的 RAG，这里的建议依旧是作为概念，先理解什么是知识图谱，为什么和 RAG 能结合？优势和劣势是啥？我就只学到这里。如果哪天它变得很火了，考的多了，可以继续学）
#### 5.2.1什么是知识图谱
知识图谱是一种结构化的知识表示形式，通过图模型来描述现实中的实体及其之间的关系。核心包括：

**节点**：表示实体或者概念，如“北京”、“小刘”  
**边**：表示实体间的关系，比如“小刘-就职于-阿里”  
**属性**：描述节点的额外信息，如“北京-人口-2154万”等。

知识图谱通过三元组的形式组织数据，从而构建出一个庞大的网络

如果用户愿意，我也可以继续把这段整理成一版更适合记忆的“极简定义版


#### 5.2.2 知识图谱在RAG的应用

传统的 RAG 是基于分块、向量召回的思路来对输入数据增强的。但是对于强调跨文档/多文档综合、需要多跳推理/链路关系的时候，可以将知识图谱和 RAG 结合，即 KG-RAG。它的做法是先用普通语义检索拿到一批 chunk，然后把这些 chunk 映射到知识图谱的实体上，沿着图的边做扩展检索，把原本鼓励的文本片段补充成一条/多条与问题相关的关系链，最终按图结构组织起来的片段交给 LLM。（简单说，向量召回的结果作为起点，KG 扩展把点连成线，再将整合结果送入 LLM）

#### 5.2.3 知识图谱vs普通RAG
普通 RAG 落地快、成本低，适合“答案就在某几段文档里”的定位性问题，但是它的弱点是需要跨多端/多条文档做综合、需要多跳推理和关系链路时，也只能拿到碎片，此种场景适合使用 KG-RAG。但是它的缺点是：需要抽取/维护实体关系，做图扩展与组织，构建与维护成本更高




## 6. RAG进阶-Agentic RAG

### 6.1 Agentic RAG是什么? 解决什么问题?
#### 6.1.1 Agentic RAG是什么?
`Agentic RAG` 实际上就是指在传统 RAG 基础上，加入了 Agent 组件的 RAG 系统. 以Agent作为控制器, 给予其一堆工具\[检索,读取元数据,读取chunk\],在循环中执行ReAct框架

#### 6.1.2 Agentic RAG在解决什么问题?
传统的Native RAG存在以下问题:
- 一次性流水线：通常“检索→拼接→生成”一步到位，没有让模型根据需要调整检索策略、递进式地钻研文档
- 缺乏任务拆解：问题可能需要先定位文件、再选片段、再比对与总结；Native RAG 往往缺少这样的多步拆解能力
- 工具编排不足：只会相似度检索，不会进一步查看文件元数据、选择需要阅读的 chunk，更不会在不够时换一种检索或补充查询
- 证据利用浅：Top-K 拼接容易“糊”上下文，无法进行“先粗后细”的证据收集（coarse→fine），也不容易明确引用到具体片段
- 适应性差：面对多跳问题（multi-hop）或信息不足的场景，通常不会回溯重试、改写查询、换路子继续找
#### 6.1.3 Agentic RAG样例
这里我想通过一个例子让读者理解什么是 `Agentic RAG`。

---

样例 1： 场景：用户问“LangChain 框架的函数调用功能怎么实现？”，而知识库文档使用的是“Function Calling/Tool Calling”等英文术语，导致首次中文检索相关度很低。

传统 RAG 的表现：

```
用户查询: “LangChain 框架的函数调用功能怎么实现？”
向量检索: 基于“LangChain”“函数调用”做相似度搜索
检索结果: 返回3个chunk，但都不够相关（最高分 0.65）
生成结果: “抱歉，未找到关于 LangChain 函数调用功能的具体信息...”
复制
```

Agentic RAG 的表现（每一轮都是 LLM 根据检索反馈自动调整关键词与表达）：

```
第1轮搜索：
- 工具：query_knowledge_base("LangChain 函数调用 功能 实现")
- 观察：命中低、证据不足
- 决策：尝试改写查询词

第2轮搜索（术语同义转换）：
- 推理：“函数调用”常见英文术语为 “Function Calling”
- 工具：query_knowledge_base("LangChain Function Calling")
- 观察：命中高相关chunk（相似度 0.89），含实现细节

第3轮搜索（可选补充）：
- 工具：query_knowledge_base("LangChain Tool Calling")
- 观察：补充工具使用相关文档


回答：
- 基于已读取片段整理实现步骤，包含“函数调用”的最小实现片段与说明。
复制
```

---

样例 2：这里只描述 Agentic RAG 的表现，不展示具体实现。 场景：用户提问“LangChain 框架的函数调用功能怎么实现？”，但是由于策略问题可能导致分块很细，导致检索的内容不完整。

```
Agentic RAG 的表现：
第1轮搜索：
- 工具：query_knowledge_base("LangChain 函数调用 功能 实现")
- 观察：命中 chunk 8 （这个 Chunk 可能不完整）
- 决策：chunk8 包含了函数调用的实现细节，但是上下文有些不完整，所以我需要读取 chunk8 前后两个 chunk 来补充上下文。

第2轮搜索（补充上下文）：
- 工具：read_file_chunks([{"fileId": 42, "chunkIndex": 7}, {"fileId": 42, "chunkIndex": 9}])
- 观察：读取到 chunk 7 与 chunk 7，包含了完整的函数调用实现细节。
- 决策：基于 chunk 7/8/9 中的信息，进行回复。

回答：
- 基于 chunk 7/8/9 中的信息，整理实现步骤，包含“函数调用”的最小实现片段与说明。
```

### 6.2 基于Agentic RAG的工业项目
#### 6.2.1 基于提示词和工具的Agentic RAG
ReAct 是一个常见的 Agent 实现方式，因此只要给 LLM 配备合适的 `Tool`以及适当的引导 `Prompt`，就可以将一个 `Native RAG` 转换成 `Agentic RAG`。这里我通过解读 `36.8k star`开源企业级项目——[chatbox](https://github.com/chatboxai/chatbox)来讲解一个 Agentic RAG 是怎么实现的，以及为什么它在复杂场景下效果好[[1]](https://yuanchaofa.com/post/from-native-rag-to-agentic-rag#fn1)。

下面是 `Chatbox` 的整体流程图，可以分为两个部分，左半部分是 `Agentic RAG`，右半部分是介于 `Native RAG` 到 `Agentic RAG`之间的 `Native RAG`

因此我们重点来解读 `chatbox` 到底是怎么[设置工具](https://github.com/chatboxai/chatbox/blob/9e33c9f998ebf240f31bbb439a430b4d5e5bd3e0/src/renderer/packages/knowledge-base/tools.ts#L78)，来实现更好的 `Agentic Search`，然后再给出最小示例代码：

> 包括 Anthropic 的 context engineering 文章中也提到了`Agentic Seach` 对于 Agent 应用是非常重要的。

- `query_knowledge_base`
    - 在知识库中进行语义搜索，快速找到候选文件或片段的“线索”。通常作为最基础的检索工具
- `get_files_meta`
    - 查看候选文件的元信息（如文件名、大小、chunk 数量），帮助模型决定“读哪几个文件的哪部分”。
- `read_file_chunks`
    - 按文件 `ID + chunkIndex` 精读具体片段，用于“取证”。建议一次只读少量最相关的 `chunk`，以降低噪声。
- `list_files`
    - 列出知识库中的文件清单，作为兜底浏览或当搜索线索不充分时的探索手段
    
#### 6.2.2 基于强化学习的Agentic RAG
Search-R1 的核心特点包括：

1. **推理与搜索的交织**：模型可以在推理过程中多次生成搜索查询，获取信息后继续推理，形成"推理-搜索-推理"的循环
2. **自主决策**：模型自主决定何时需要搜索、搜索什么内容，而不是按照固定流程执行
3. **多轮搜索交互**：支持在单次推理过程中进行多轮搜索，每轮搜索都基于当前的推理状态
4. **检索令牌掩码**：使用检索令牌掩码技术确保 RL 训练的稳定性
5. **基于结果的奖励函数**：采用简单的基于结果的奖励函数，鼓励模型生成正确答案

#### [4.4.2 强化学习训练流程](https://yuanchaofa.com/post/from-native-rag-to-agentic-rag#442-%E5%BC%BA%E5%8C%96%E5%AD%A6%E4%B9%A0%E8%AE%AD%E7%BB%83%E6%B5%81%E7%A8%8B)

Search-R1 的训练流程主要包括以下步骤：


## 7. RAG其他问题
~~~
Q1:RAG已死? 随着大模型长上下文窗口能力的提升，社区中开始出现“RAG 已死”的声音.一是认为长上下文已经能暴力“消化”海量文本，不再需要复杂的检索系统.二是批评 RAG 这个术语本身就过于宽泛，模糊了太多技术细节，反而阻碍了理解与优化

A:其实RAG这个概念本身也是在不断吸收新技术, 不断进化和泛化的.我们可以拿Transformer来进行类比,不论是GPT的Decorder-only还是Bert的Encorder-only,其实也早就不是2017年Attention is all you need原论文当中的架构了.
RAG的核心思想是将将LLM的内在参数化知识与外部非参数化知识相结合.其本身其实也是在不断推陈出新的,比如Agentic RAG以及Graph RAG. 所以我认为RAG已死是一个伪命题,它作为一个概念活得很好，它正在像 Transformer 一样，成为一个不断吸收新技术、不断进化的基础架构范式

~~~
~~~
Q2:如果用户做 RAG 召回的相关内容里，会有人恶意注入了一些错误的信息，用户觉得会影响大模型的生成内容吗？怎么避免？

A:会影响生成内容。我认为：
1. 可以限制拿到的数据的来源，只选择可靠的数据源
2. 可以对拿到的数据进行安全性检验，检验是否包含恶意指令
~~~

## 8.参考资料
- **All in RAG** https://datawhalechina.github.io/all-in-rag/#/chapter2/04_data_load